@using Framework.MVC.Resources
@using Topppro.Entities.Resources

@model Topppro.Entities.Assn_CategorySerieProduct

@using (Ajax.BeginForm("Edit", null, null,
        new AjaxOptions()
        {
            OnSuccess = "el.replaceWith(data); $('.bootstrap-dialog').modal('hide');"
        }))
{
    @Html.ValidationSummary(true, null, new { @class = "alert alert-danger" })
    <fieldset>
        @Html.EditorForModel("AssnCategorySerieProduct")
    </fieldset>
}
