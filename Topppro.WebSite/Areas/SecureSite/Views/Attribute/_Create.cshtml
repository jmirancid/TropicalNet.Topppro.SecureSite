@using Framework.MVC.Resources
@using Topppro.Entities.Resources

@model Topppro.Entities.Attribute

@using (Ajax.BeginForm("Create", "Attribute", null,
        new AjaxOptions()
        {
            OnSuccess = "$('#attribute-list').dataTable().fnAddData(data); $('.bootstrap-dialog').modal('hide');"
        }))
{
     @Html.ValidationSummary(true, null, new { @class = "alert alert-danger" })
    <fieldset>
        @Html.EditorForModel()
    </fieldset>
}
