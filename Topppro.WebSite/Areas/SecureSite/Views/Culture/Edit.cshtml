@using Framework.MVC.Resources
@using Topppro.Entities.Resources

@model Topppro.Entities.Culture

@{
    ViewBag.Title = PersistanceResource.Edit;
}

@section Styles {
    <style type="text/css">
        .tooltip-inner
        {
            max-width: 300px;
            padding: 0;
        }
        .tooltip-alert
        {
            padding: 8px 10px;
            font-weight: bolder;
            color: #A94442;
        }
    </style>
}

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>@Ent_CultureResource._Caption</legend>

        @Html.HiddenFor(model => model.CultureId)
        @Html.EditorForModel()

        <p>
            <input class="btn btn-primary" type="submit" value="@PersistanceResource.Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Html.Partial("_Validation")
}
