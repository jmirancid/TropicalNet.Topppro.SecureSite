@using Topppro.Entities.Resources

@model IEnumerable<Topppro.Entities.Package>

@{
    Html.RenderAction("Toolbar", "Package", new { id = ViewBag.ParentProductId });
}

<table class="table table-striped table-bordered table-hover" id="package-list">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ChildProduct.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InheritAttribute)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InheritHiRes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Priority)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Enabled)
            </th>
            <th>
                Actions
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.ChildProduct.Name)</td>
                <td>@item.InheritAttribute.ToString().ToLower()</td>
                <td>@item.InheritHiRes.ToString().ToLower()</td>
                <td>@Html.DisplayFor(modelItem => item.Priority)</td>
                <td>@item.Enabled.ToString().ToLower()</td>
                <td class="center">@Html.DisplayFor(modelItem => item.Id)</td>
            </tr>
        }
    </tbody>
</table>

<script type="text/javascript">
    $(document).ready(function () {
       
        $('#package-list').dataTable({
            aoColumns: [
                    null,
                    {
                        mRender: function (o, t, r) {

                            var tpl = '@Html.CheckBox("InheritAttribute", true, new { data_id = 0 })';

                            if (JSON.parse(o) === false)
                                tpl = '@Html.CheckBox("InheritAttribute", false, new { data_id = 0 })';

                            return tpl.replace(/0/g, r[5]);
                        }
                    },
                    {
                        mRender: function (o, t, r) {

                            var tpl = '@Html.CheckBox("InheritHiRes", true, new { data_id = 0 })';

                            if (JSON.parse(o) === false)
                                tpl = '@Html.CheckBox("InheritHiRes", false, new { data_id = 0 })';

                            return tpl.replace(/0/g, r[5]);
                        }
                    },
                    null,
                    {
                        mRender: function (o, t, r) {

                            var tpl = '@Html.CheckBox("Enabled", true, new { data_id = 0 })';

                            if (JSON.parse(o) === false)
                                tpl = '@Html.CheckBox("Enabled", false, new { data_id = 0 })';

                            return tpl.replace(/0/g, r[5]);
                        }
                    },
                    {
                        bSortable: false,
                        mRender: function (o) {

                            var tpl = '@Html.ActionLink("Edit", "Edit", "Package", new { id = 0 }, new { @class = "editor-dialog", @role = "button", @type = "type-warning", @title = string.Format("{0} {1}", "Edit", Ent_BulletResource._Caption) }) | ' +
                                      '@Html.ActionLink("Details", "Details", "Package", new { id = 0 }, new { @class = "display-dialog", @role = "button", @type = "type-warning", @title = string.Format("{0} {1}", "Details", Ent_BulletResource._Caption) }) | ' +
                                      '@Ajax.ActionLink("Delete", "Delete", "Package", new { id = 0 }, new AjaxOptions() { HttpMethod = "Post", Confirm = "Confirm Delete ?", OnSuccess = "$('#package-list').dataTable().fnDeleteRow(el);" }, new { @role = "button", @onclick = "el = $(this).closest('tr');" })';
                            
                            return tpl.replace(/0/g, o);
                        }
                    }
                ]
        });

        $('#package-list tbody').on('change', 'input[type=checkbox]', function(e) {
            
            var o =
                $(this);

            $.post('@Url.Action("Toggle", "Package")',
                { id: o.data('id'), propertyName: o.attr('name') });
        });
    })
</script>
