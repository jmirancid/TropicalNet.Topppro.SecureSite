@model Topppro.Entities.Bullet

@using (Ajax.BeginForm("Edit", "Bullet", null,
        new AjaxOptions()
        {
            OnSuccess = "$('#bullet-list').dataTable().fnUpdate(data, el[0]); $('.bootstrap-dialog').modal('hide');"
        }))
{
    @Html.ValidationSummary(true, null, new { @class = "alert alert-danger" })
    <fieldset>
        @Html.EditorForModel()
    </fieldset>
}
