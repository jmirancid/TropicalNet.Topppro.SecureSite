@model Topppro.SecureSite.Models.ImportViewModel

@using (Html.BeginForm("Import", "Attribute", FormMethod.Post))
{
     @Html.ValidationSummary(true, null, new { @class = "alert alert-danger" })
    <fieldset>
        @Html.EditorForModel()
    </fieldset>
}
