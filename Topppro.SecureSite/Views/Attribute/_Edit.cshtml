@model Topppro.Entities.Attribute

@using (Ajax.BeginForm("Edit", "Attribute", null,
        new AjaxOptions()
        {
            OnSuccess = "$('#attribute-list').dataTable().fnUpdate(data, el[0]); $('.bootstrap-dialog').modal('hide');"
        }))
{
    @Html.ValidationSummary(true, null, new { @class = "alert alert-danger" })
    <fieldset>
        @Html.EditorForModel()
    </fieldset>
}
